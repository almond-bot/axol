---
title: "zed.install"
description: "Install the pyzed Python wheel matching the installed ZED SDK version."
---

Downloads and installs the `pyzed` Python wheel matching the installed ZED SDK version. Caches the wheel in `~/.almond/wheels/`.

```bash
axol zed.install
```
