export const F = ({ children }) => <code style={{ whiteSpace: "nowrap" }}>{children}</code>;

| Flag | Default | Description |
|---|---|---|
| <F>--{prefix}rest_pose_left</F> | `[−0.157, 0, 0, 0.314, 0, 0, −0.157]` | Left arm rest pose (rad, 7 joints). The arms start here and return here on reset. Edit it visually in the control panel's **Rest pose** tab. |
| <F>--{prefix}rest_pose_right</F> | `[0.157, 0, 0, −0.314, 0, 0, 0.157]` | Right arm rest pose. |
| <F>--{prefix}frequency</F> | `120` | Control-loop rate (Hz). Data collection sets this from `teleop_hz`. |
| <F>--{prefix}position_multiplier</F> | `1.0` | Scales hand movement. `2` moves the arm twice as far as your hand. Ignored in `absolute_mode`. |
| <F>--{prefix}rotation_multiplier</F> | `1.0` | Scales hand rotation the same way. Ignored in `absolute_mode`. |
| <F>--{prefix}hold_to_engage</F> | `false` | `false`: click both grips to engage, then click a grip to freeze or unfreeze that arm. `true`: each arm tracks only while its grip is held. |
| <F>--{prefix}disengage_timeout</F> | `0.5` | Seconds without a VR frame before tracking disengages and the arms hold position. `0` disables. |
| <F>--{prefix}reset_speed</F> | `0.628` | Speed (rad/s) of the return-to-rest move. |
| <F>--{prefix}reset_min_duration</F> | `1.5` | Minimum duration (s) of a return-to-rest move. |
| <F>--{prefix}reset_rest_weight</F> | `50.0` | IK weight toward the rest pose when planning a return. |
| <F>--{prefix}reset_limit_weight</F> | `100.0` | IK weight against joint limits when planning a return. |
| <F>--{prefix}reset_collision_margin</F> | `0.025` | Minimum self-collision clearance (m) when planning a return. |
| <F>--{prefix}reset_collision_weight</F> | `100.0` | IK weight on self-collision when planning a return. |
| <F>--{prefix}reset_max_iterations</F> | `10` | IK iterations per return waypoint. |
| <F>--{prefix}reset_torque_threshold</F> | `4.0` | **Contact stop during return-to-rest** (Nm of unexpected torque). If the arm hits something, the move stops and the arms go limp; press reset to retry. `0` disables. Hardware only. |
| <F>--{prefix}teleop_torque_threshold</F> | `0` (off) | **Contact stop while tracking.** Same behaviour; a running episode is discarded. The control panel arms it at `16`. Hardware only. |
| <F>--{prefix}reset_gravity_comp_kd</F> | `0.25` | Damping (Nm·s/rad) of the limp hold after a contact stop. |
| <F>--{prefix}engage_max_vel</F> | `0.628` | Joint speed cap (rad/s) right after engaging, so the first move is gentle. |
| <F>--{prefix}engage_duration</F> | `1.0` | Seconds to ramp from `engage_max_vel` up to `teleop_max_vel`. |
| <F>--{prefix}teleop_max_vel</F> | `6.283` | Maximum joint speed (rad/s) while tracking. |
| <F>--{prefix}teleop_max_accel</F> | `21.99` | Maximum joint acceleration (rad/s²) while tracking. |
| <F>--{prefix}ik_alpha</F> | `0.3` | Smoothing on the IK output, `(0, 1]`. `1` is no smoothing; lower is smoother but adds lag. See [Latency tuning](/operations/teleop#latency-tuning). |
| <F>--{prefix}pose_min_cutoff</F> | `0.8` | Hand-tremor filter (Hz). Lower is smoother at rest but adds lag. Typical range 0.5–3. |
| <F>--{prefix}pose_beta</F> | `2.0` | Lets fast, intentional moves through the tremor filter. Increase if fast moves feel sticky. |
| <F>--{prefix}absolute_mode</F> | `false` | Mantis mapping: track controller poses 1:1 in a fixed world frame instead of relative to where you engaged. Forced `true` by `--mantis`. |
| <F>--{prefix}base_height</F> | `null` | In `absolute_mode`, fix the robot base this many metres above the VR floor instead of inferring it. Keeps datasets consistent across operators. |
| <F>--{prefix}tcp_transform_left</F> | `null` | *Mantis data collection.* Tracker → gripper transform `[x, y, z, qx, qy, qz, qw]` for the left rig. Normally read from the saved calibration; set to override. See [`tracker`](/cli/tracker#transform-file). |
| <F>--{prefix}tcp_transform_right</F> | `null` | *Mantis data collection.* Same for the right rig. |
| <F>--{prefix}tracker_key</F> | `null` | *Mantis data collection, Quest.* Which saved calibration to use, e.g. `quest:oculus-touch-v3:grip`. Set from **Mantis settings** in the panel. |
| <F>--{prefix}quest_controller_profile</F> | `null` | *Mantis data collection, Quest.* Expected controller profile. Filled from `tracker_key`. |
| <F>--{prefix}quest_pose_space</F> | `null` | *Mantis data collection, Quest.* Expected pose datum, `grip` or `target-ray`. Filled from `tracker_key`. |
| <F>--{prefix}urdf_viewer_world_aligned</F> | `null` | *Mantis data collection.* Whether the pose source shares the viewer headset's floor. `null` means `true` for Quest, `false` for Lighthouse/Ultimate. |

<Note>
  **Mantis mode** (`--mantis`) forces `absolute_mode: true`, `hold_to_engage: false`, `ik_alpha: 1.0`, `pose_min_cutoff: 5.0`, and removes the speed and acceleration limits. Your values for these keys are ignored in Mantis mode.
</Note>
