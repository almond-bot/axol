[package]
name = "axol-rt"
version = "0.1.0"
edition = "2021"
description = "Realtime CAN control core for the Almond Axol arms"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 3
lto = true
